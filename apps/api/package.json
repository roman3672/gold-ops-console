{
  "name": "@gold-ops/api",
  "version": "0.0.0",
  "private": true,
  "description": "Fastify API — product + ops boundary",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "license": "ISC",
  "dependencies": {
    "@gold-ops/sdk": "workspace:*",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "tsx": "^4.19.3",
    "typescript": "~5.8.3"
  }
}
